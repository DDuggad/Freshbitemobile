
  # FreshBiteMobile

  This is a code bundle for FreshBiteMobile. The original project is available at https://www.figma.com/design/STYJhKytEvMCETtOwLcuSd/FreshBiteMobile.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  